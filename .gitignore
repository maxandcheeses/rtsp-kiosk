# Runtime config — contains credentials, never commit
data/streams.json
data/streams-public.json
data/views-public.json
data/mediamtx.yml

# Environment variables — contains host IP
.env

# Build artifacts
node_modules/
www/mqtt.min.js