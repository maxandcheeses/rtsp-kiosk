---
allowed-tools: Bash(git:*)
---

Summarize the current git state:
1. Run `git status`
2. Run `git diff HEAD`
3. Run `git log --oneline -10`
4. Report: current branch, uncommitted changes (files + summary of what changed), last 10 commits
